[package]
name = "accession"
description = "The MTH.C/MTH.R accession scheme: parsing, minting and citation strings (SPEC.md 5)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
